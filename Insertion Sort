#include<stdio.h>
#include<math.h>

void insertionSort(int arr[], int n)
    {
        for (int a = 1; a < n ;a++){
            int key = arr[a];
            int j = a-1;
            while(j >= 0 && arr[j] > key ){
                arr[j+1] = arr[j];
                j--;
            }arr[j+1] = key;     
        }
    }
void printarray(int arr[],int size)
{
    int i;
    for(i=0;i<size;i++)
        printf("%d ",arr[i]);
    printf("\n");    
}
int main()
{
    int arr[1000],n,i;
    scanf("%d",&n);
    for(i=0;i<n;i++)
        scanf("%d",&arr[i]);
    insertionSort(arr, n);
    printarray(arr, n);
    return 0;
}
